// Create an object named ‘student’ with properties: ‘name’ as ‘Ali’, ‘age’ as 20, and ‘grade’ as ‘A’. Print the name and grade of the student.


let student = {
    name: "Ali",
    age: 20,
    grade: "A",
}

let result = `${student.name} acheived ${student.grade} Grades in the examination.`
console.log(result);